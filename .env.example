# 评测环境配置模板。所有 API key 与接入点 URL 都由 .env 管理，不写进代码。
#
# 用法：
#   cp .env.example .env
#   # 编辑 .env，填入真实的 key 与 URL
#
# .env 已被 .gitignore 排除，不会提交；本模板不含任何真实值，可以提交。
# 本文件所有值一律留空 —— 接入点是部署相关的，不应有默认值。
#
# run_eval.sh 在应用任何默认值之前加载 .env，且只填充尚未设置的变量，
# 因此已 export 的同名环境变量优先级更高，临时覆盖仍可用：
#   JUDGE_API_KEY=... bash eval/rpbench/run_eval.sh

### judge（必填，缺任意一项 run_eval.sh 会直接报错退出）
# 给 eval/rpbench/judge.py 打分用。付费接口。
# 示例：JUDGE_MODEL=gpt-5.6-sol
JUDGE_API_KEY=
JUDGE_MODEL=
JUDGE_URL=

### user simulator（必填 USER_MODEL）
# 扮演用户的模型。所有被测模型必须面对同一个 user simulator，否则分数不可比。
# 示例：USER_MODEL=gpt-5.2-chat
# judge 与 user 是各自独立的接入点，不共用 URL/key；两者可以是不同网关。
# USER_BASE_URL 留空表示复用本地 vLLM，此时 USER_MODEL 必须是它 serve 的名字（如 base），
# 且 NPC_MODELS 中至少要有一个本地条目，否则没有 server 可复用（会直接报错）。
# 走远程时按轮计费，调用量约为 judge 的 10 倍（每个 user 轮次一次，且要发全量历史）。
# 设了 USER_BASE_URL 就必须同时设 USER_API_KEY。
USER_MODEL=
USER_BASE_URL=
USER_API_KEY=

### 被测模型
# 空格分隔。裸名 = 本地 vLLM 上的模型（仅支持 base / sft / dpo）；
# label=model-name = 远程 API，label 是结果中记录的名字。
# 只要含裸名就启动 vLLM 并校验对应 adapter，全是远程条目则完全不占 GPU。
# 留空是错误而非默认值 —— 想跑默认三个就整行注释掉。
#
#   NPC_MODELS=gpt52=gpt-5.2-chat fable=claude-fable-5   # 纯远程，无需 GPU 和 adapter
#   NPC_MODELS=base sft dpo                              # 纯本地
#   NPC_MODELS=base sft gpt52=gpt-5.2-chat               # 混合，只挂载 sft adapter
#
# 远程侧的已知限制：chat_template_kwargs 是 vLLM 扩展，托管网关不认，
# 因此远程模型的思维链无法强制关闭；若模型默认输出思考过程会被 basics 维度扣分。
# 远程 seed 也只是 best-effort，重跑轨迹可能与上次不同。
# NPC_MODELS=
# NPC_BASE_URL=   # 含远程条目时必填
# NPC_API_KEY=

# 配对比较的基准模型。必须是 NPC_MODELS 里的某个 label，否则跳过配对比较。
# BASELINE=base

### 训练日志上报 swanlab（可选，scripts/run_train.sh 读取）
# 填好 key 后跑一次 `bash scripts/swanlab_login.sh` 坐实凭证 —— 它会真的打一次
# 服务端，能抓出"格式合法但 key 无效"这种 run_train.sh 的本地预检查不出来的情况。
# 登录后凭证存在 ~/.swanlab/.netrc，之后即使不带 .env 也能上报。
# 不填 SWANLAB_API_KEY 时 run_train.sh 会在加载模型之前就拒绝启动，
# 而不是等十几分钟后在 swanlab.init() 崩掉（没有 TTY 时它会尝试交互式登录并失败）。
# 三种规避方式：
#   SWANLAB_MODE=local bash scripts/run_train.sh   # 只写本地，swanlab watch 查看
#   SWANLAB=0 bash scripts/run_train.sh            # 完全不上报
#   在此填入 SWANLAB_API_KEY
# SWANLAB_RUN_ID 不要手动设：run_train.sh 由 output_dir 推导并固定它，
# 这样断点续训会续到同一条曲线上，而不是每次重启开一条新实验。
#
# 注意项目名的变量叫 SL_PROJECT，不叫 SWANLAB_PROJECT。swanlab 0.9.7 用
# pydantic-settings 读环境变量（前缀 SWANLAB_），其中 project 是嵌套字段，
# 塞字符串会让 swanlab.init() 直接抛 SettingsError 把训练打断。而且 swanlab
# 会自己读当前目录的 .env —— 所以这一行写在这个文件里同样会踩雷，别加回来。
# run_train.sh 启动前会预检一次，配错了会当场报错而不是十几分钟后崩。
# SWANLAB_API_KEY=
# SL_PROJECT=seaart-roleplay
# SWANLAB_MODE=

### conda 环境（可选）
# 训练环境（torch 2.8.0）与 vLLM 环境（torch 2.13）必须分开。
# CONDA_SH 留空时从 PATH 上的 conda 自动推导；conda 不在 PATH 上才需要手动指定。
# CONDA_SH=
# VLLM_ENV=rpbench
# CLIENT_ENV=roleplay

### 评测规模（可选）
# NUM_TURNS=40
# RUNS=1
# MAX_SEEDS=0
# 默认已按 user model 命名（results_<user-model>/），换 user simulator 时自动隔离，
# 因为不同 user simulator 下的分数不可比。一般不需要手动设置。
# RESULTS_DIR=
